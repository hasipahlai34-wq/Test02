# ============================================================
# 自适应RAG文档问答系统 — Python 依赖
# 安装: pip install -r requirements.txt
# ============================================================

# LangGraph 工作流编排（← WeKnora engine.go 自研ReAct循环）
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0

# 向量数据库（← WeKnora pgvector → ChromaDB）
chromadb>=0.5.0

# 文档解析（← WeKnora gRPC docreader → LangChain Loaders）
pymupdf>=1.24.0
python-docx>=1.1.0
docx2txt>=0.8
markdown>=3.6
unstructured>=0.15.0
pandas>=2.0.0

# 检索增强（← WeKnora BM25 + Rerank）
rank-bm25>=0.2.2
sentence-transformers>=3.0

# 配置与数据模型
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0

# YAML Prompt 管理（← WeKnora prompts.go）
pyyaml>=6.0
jinja2>=3.1

# API 与 UI
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
streamlit>=1.35.0

# Token 估算（← WeKnora token/estimator.go → tiktoken）
tiktoken>=0.7.0

# 评估体系 (RAGAS)
ragas>=0.2.0
datasets>=3.0
langchain-google-vertexai>=3.0  # ragas 0.4.x 兼容: ChatVertexAI 已从 langchain-community 迁移至此

# 工具库
rich>=13.0
httpx>=0.27.0
tenacity>=9.0
structlog>=24.0

# 开发依赖
pytest>=8.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0
